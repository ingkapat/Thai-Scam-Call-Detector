// Backend endpoint for real inference.
//   "http://127.0.0.1:8000"  -> local FastAPI backend (TA-style)
//   ""                        -> demo mode (canned data, no backend)
window.API_URL = "https://paam1-thai-scam-detector.hf.space";
